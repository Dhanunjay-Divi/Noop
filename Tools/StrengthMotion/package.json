{
  "name": "noop-strength-motion",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild viewer.ts body-map.ts --bundle --minify --sourcemap --target=es2020 --outdir=dist && esbuild guidance.ts --bundle --minify --sourcemap --target=es2020 --platform=node --format=esm --outfile=dist/guidance.js && node build-native-body-map.mjs",
    "check": "tsc --noEmit",
    "qa": "npm run check && npm run build && node qa.mjs"
  },
  "dependencies": {
    "lucide": "0.468.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "playwright": "^1.58.0",
    "pngjs": "^7.0.0",
    "typescript": "5.7.3"
  }
}
