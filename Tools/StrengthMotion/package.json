{
  "name": "noop-strength-motion",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild viewer.ts --bundle --minify --sourcemap --target=es2020 --loader:.glb=dataurl --outfile=dist/viewer.js",
    "check": "tsc --noEmit",
    "qa": "npm run check && npm run build && node qa.mjs"
  },
  "dependencies": {
    "lucide": "0.468.0",
    "three": "0.172.0"
  },
  "devDependencies": {
    "@types/three": "0.172.0",
    "esbuild": "0.28.1",
    "playwright": "^1.58.0",
    "pngjs": "^7.0.0",
    "typescript": "5.7.3"
  }
}
