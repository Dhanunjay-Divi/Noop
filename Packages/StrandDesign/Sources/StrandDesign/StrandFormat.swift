import Foundation

/// Shared display-only formatting tokens used across NOOP surfaces.
public enum StrandFormat {
    /// A value is unavailable or not yet measured. Use words for real states such as "Never".
    public static let missing = "—"
}
